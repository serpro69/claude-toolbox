Local command caveats (appear around slash-command invocations like `/effort`, `/export`):

```
<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>
<command-name>/effort</command-name>
            <command-message>effort</command-message>
            <command-args>high</command-args>
<local-command-stdout>Set effort level to high: Comprehensive implementation with extensive testing and documentation</local-command-stdout>
```

Observed variants this session: `/effort high`, `/effort xhigh`, `/export wtf.txt`.

These are harness-generated envelopes — not something you type directly. The `caveat` line instructs me to ignore the wrapped stdout unless you address it.
