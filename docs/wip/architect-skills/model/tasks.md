# Tasks — Milestone 2 `/kk:model`

> **Design:** [design.md](design.md) · **Implementation:** [implementation.md](implementation.md) · **Umbrella:** [../design.md](../design.md)
> **Status:** done
> **Reflection (M2 complete, 2026-09-03):** The build tracked the plan; the milestone's recurring theme was *boundary discipline for shipped content*. Three instances: eval-oracle hedges papering over underspecified procedures (Task 7 — name the verdict, delete the hedge), doc counts hard-coded across eight files with no single owner (Task 8 — sweep list now in `kk:project-conventions`), and — the sharpest — toolbox-ADR citations inside plugin prose, where even the pre-existing "(ADR 0004)" house pattern turned out to be the same defect: the plugin ships standalone, so repo-doc pointers are dangling or misleading for consumers (user-caught; now CLAUDE.md §Plugin content is self-contained). Producer evals stayed spec-only per convention; the review-architecture extensions were verified live (evals 8/10/11 in Task 7, eval 7 in Task 8 — the Check A clarification proved regression-free, 8/8). Open items carried forward, not dropped: the Check A under-fire control eval for kits (M2.1 backlog), the ratification-record home (design §8 → `decide`), and the go.mod module-name straggler in kit-format's fixture.
> **Not Doing:** decide producer (M2.1); decompose producer (M2.2+); M3 hand-off; process-review skill; profile `architecture/` phase (M4); /kk:design changes; freshness CI; session-close guard (falsified).

---

## Task 1 — Shared guards + `/kk:model` skeleton

**Status:** done
**Size:** M
**Depends on:** —
**Can run in parallel with:** 6
**Slicing:** Contract-First (the guard files define the producer workflow spine every later task builds on)
**Docs:** [design.md §5](design.md) · [implementation.md — Build order 1](implementation.md)

- [x] Create `klaude-plugin/skills/_shared/requirements-harvesting.md`, `open-question-pass.md`, `fact-flip-propagation.md`, `contact-ratio-guard.md` — each opens with its motivating failure (design §1 F1/F4/F2/F5), then the rule as a mandatory workflow step, written producer-generic (consumable by `decide` later without edits).
- [x] Create `klaude-plugin/skills/model/SKILL.md` — frontmatter (`name: model`, trigger-keyword-first description ≤1024 soft), ADR-0004 mandatory-order directive, seven-phase Workflow summary (placeholder links to files landing in Tasks 2–4 are acceptable only if `make plugin-graph` stays green — otherwise stub the files in this task). *(Stubbed `model-process.md`, `kit-contract.md`, `archaeology.md` to keep the link/orphan gate green; populated in Tasks 2–4.)*
- [x] Create the five `shared-` symlinks in `klaude-plugin/skills/model/` (`ln -s ../_shared/<name>.md shared-<name>.md`): the four new guards plus the existing `capy-knowledge-protocol.md`.
- [x] Add `model` to `EXPECTED_SKILLS` in `test/test-plugin-structure.sh`; run `make generate-kodex`.
- [x] **verify:** `bash test/test-plugin-structure.sh` green; `make plugin-graph` link/orphan gate clean; `git diff --exit-code kodex-plugin/` clean after regen. *(`make generate-kodex`'s bundled `test-codex-structure.sh` reports 7 pre-existing TOML failures — this env has Python 3.10 with no `tomllib`/`tomli`; unrelated to this change, `.codex/` regen is idempotent.)*

## Task 2 — Kit output contract + kit-format eval

**Status:** done
**Size:** M
**Depends on:** 1
**Can run in parallel with:** 6
**Docs:** [design.md §3](design.md) · [implementation.md — Build order 2](implementation.md)

- [x] Create `klaude-plugin/skills/model/kit-contract.md` — glossary page format (dual-audience entry fields, status markers, rules table + provenance banner, one conceptual-altitude diagram, Derived-from footer, no line numbers, ticket-note retirement discipline, 10–20 term guideline with variance drivers), traps page format (P#/D# stable numbering, no schema mirror, self-limiting entries), conventions-index content (once per home), freshness banner (proposed-until-reviewed + re-verification via `/kk:review-architecture`), homes-resolution precedence, kit-scope pushback.
- [x] Build `evals/kit-format/` — small fictional-domain code slice in `test-files/`; oracle checklist in `oracle/` covering every kit convention; assertions per convention (all intent claims `proposed`; no line numbers; footer complete; exactly one diagram; no schema mirror in traps). *(Bike-share domain slice, disjoint from the contract's library-lending examples; oracle at `oracle/kit-conventions-checklist.json` maps every §3 convention → assertion 2.1–2.13.)*
- [x] **verify:** eval JSON valid; every design-§3 convention has a corresponding assertion; structure tests + plugin-graph green. *(`test-plugin-structure.sh` 182/182; `make plugin-graph` no broken edges/orphans; `make generate-kodex` regenerated the `kodex-plugin/skills/model/` mirror, identical modulo the documented `/kk:`→`$kk:` transform — the 7 `test-codex-structure.sh` TOML failures are the pre-existing no-`tomllib` env issue from Task 1, `.codex/agents/` unchanged.)*
- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). Applied: seeded a neutral `TODO(BIKE-204)` in `dock.go` + added assertion **2.14** (ticket-note retirement discipline — previously an untested §3 convention: no ticket in the fixture meant a model never emits a ticket note); clarified **2.7** so `go.mod` (no domain fact) isn't required in the Derived-from footer; softened the oracle term-count band to name the 4 core concepts (removes grader ambiguity); annotated the `canonical` example in `kit-contract.md` so it can't contaminate assertion 2.5 (greenfield defaults to `proposed`). Rejected: the "backticks impossible in JSON" 2.10 nit (false — 2.10 uses backticks); "add a regression eval" (that is Task 5 `scope-pushback`). Two systemic eval-authoring lessons indexed to `kk:review-findings`.

## Task 3 — Production workflow + requirements-gate & hidden-invariant evals

**Status:** done
**Size:** M
**Depends on:** 2
**Can run in parallel with:** 6
**Docs:** [design.md §4](design.md) · [implementation.md — Build order 3](implementation.md)

- [x] Create `klaude-plugin/skills/model/model-process.md` — the seven phases (greenfield path): scope intake + forcing question + capy search per the shared protocol (`kk:arch-decisions`, `kk:project-conventions`), requirements gate (invokes the shared guard), archaeology (links `archaeology.md`), kit drafting (links `kit-contract.md`), verification (confirm-pass + open-question pass via shared guard), conventions-bind-author self-check, surface & close (RQ queue with role deciders, appended to the feature's `docs/wip/<feature>/design.md` **Open Questions** section when the feature dir exists; contact-ratio guard; capy indexing of non-obvious rationale). *(Dropped the stub's `docs/wip/` relative link — the path doesn't exist in an installed plugin; brownfield intake/close specifics deferred to Task 4 via an HTML-comment note in the file.)*
- [x] Create `klaude-plugin/skills/model/archaeology.md` — state → time → invariants reading order; two-clock discipline; model-in-service-of-the-decision scoping.
- [x] Sync the SKILL.md phase summary with `model-process.md`; run the ADR-0004 dedup pass (no repeated content-read instructions). *(Summary already matched — no SKILL.md edit needed; dedup grep confirms the content-read instruction appears exactly once, in process phase 3.)*
- [x] Build `evals/requirements-gate/` (ticket files present; trap: modelling without quoting the asks / inventing a brief) and `evals/hidden-invariant/` (code slice hiding a single-result-lookup-class cross-file trap; trap: confirm-only verification missing it). *(Ferry vehicle-deck domain with FERRY-31 → FERRY-12 one-hop chain forcing the hop rule; campground domain with Book/OccupantOf/GateCode write-read split. Both domains disjoint from library-lending (kit-contract examples) and bike-share (kit-format). Oracles `gold-harvest.json` / `gold-invariant.json` in grader-only `oracle/` dirs; widened the structure test's oracle-leak glob `gold-claims*.json` → `gold-*.json` to cover the new oracle name class.)*
- [x] **verify:** phase summary and process file agree; both evals' traps map to F1/F4; assertions decidable from output text alone; all gates green. *(`test-plugin-structure.sh` 182/182; `make plugin-graph` no broken edges/orphans; kodex regen idempotent with the `/kk:`→`$kk:` transform applied — the 7 `test-codex-structure.sh` TOML failures remain the pre-existing no-`tomllib` env issue from Task 1; Go fixtures `gofmt`-clean; eval/oracle JSON valid.)*
- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). Applied: `var used int` zero-value style in `sailing.go`; `Book` returns `Booking` by value (pal-found pointer-into-slice red herring that could distract the model under test from the seeded overlap trap); tightened assertion 3.1's failing-condition wording (eval-grading vs run-failure ambiguity). Rejected: renumbering eval ids (ids 1–5 follow design §7's eval ordering — kit-format already took id 2 in Task 2 under the same scheme); rewriting the phase-7 RQ example out of the library-lending domain (the suggested campground rewrite would have embedded the hidden-invariant eval's expected RQ verbatim in an instruction file — instruction examples stay in the one burned illustration domain; systemic lesson indexed to `kk:review-findings`).

## Task 4 — Brownfield mode + brownfield-update eval

**Status:** done
**Size:** M
**Depends on:** 3
**Can run in parallel with:** 6, 7
**Docs:** [design.md §4 (intake, close)](design.md) · [implementation.md — Build order 4](implementation.md)

- [x] Extend `model-process.md`: brownfield intake (read existing kit in full first; baseline + deltas; never flip status markers unilaterally) and brownfield close (fact-flip propagation on contradicted recorded facts). *(New §Brownfield intake in phase 1 — baseline read, delta discipline, enforcement-≠-ratification marker rule; phase 7 item 3 gains the three contradicted-fact shapes: stale binding, vanished enforcement, met retirement condition. SKILL.md summary already matched — no edit needed; dedup grep confirms the domain-source read instruction still appears exactly once, in phase 3.)*
- [x] Build `evals/brownfield-update/` — existing fictional-domain kit + drifted code slice; ≥2 seeded stale assertions in distinct files; a `proposed` entry a naive run would "promote". *(Climbing-gym routesetting domain, disjoint from library-lending/bike-share/ferry/campground. Three seeded drifts: `Route.Difficulty`→`Route.Grade` rename asserted in both kit pages (F2 fact-flip across files), `gym/archive.go` purge deleted while L3 + footer still cite it, and `OpenRoute` now enforcing `proposed` L1 as the promotion bait — with traps D1's retirement condition met. Oracle at `oracle/gold-deltas.json`.)*
- [x] **verify:** assertions cover (a) every seeded stale assertion caught, (b) no unilateral `proposed → canonical` flip, (c) delta update rather than full rewrite; all gates green. *(Coverage: (a)=4.2/4.3/4.4, (b)=4.5, (c)=4.1, retirement discipline=4.6. `test-plugin-structure.sh` 182/182; `make plugin-graph` no broken edges/orphans; kodex regen idempotent with the `/kk:`→`$kk:` transform; the 7 `test-codex-structure.sh` TOML failures remain the pre-existing no-`tomllib` env issue from Task 1; Go fixtures `gofmt`-clean; eval/oracle JSON valid.)*
- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). Applied: staging-semantics note in `eval.json` (absence of `gym/archive.go` is an observable whole-tree property, since `files[]` cannot express absence); oracle grader note that legitimate unseeded findings are ungraded. Rejected: an `archive.go.DELETED` sentinel file (would leak the graded answer); a `StripRoute` state guard (stripping a never-opened route doesn't violate L2, which constrains reopening); a mode-detection regression eval (design §7 names `scope-pushback` as the regression eval — Task 5 — and evals 1–3 already exercise greenfield with no kit present); pal's sentinel-error refactor in `route.go` (no error-flow consumers in the slice; four exported `Err*` symbols add API surface irrelevant to the seeded traps). Systemic lesson indexed to `kk:review-findings`: absence-based traps must state harness staging semantics, and never via sentinel files.

## Task 5 — Scope-pushback regression eval + description tuning

**Status:** done
**Size:** S
**Depends on:** 3
**Can run in parallel with:** 4, 6, 7
**Docs:** [design.md §7](design.md) · [implementation.md — Build order 5](implementation.md)

- [x] Build `evals/scope-pushback/` — natural prompt asking `/kk:model` for a durable full ERD / schema-mirror page; assertions require pushback naming the maintenance cost (and offering the kit or a disposable per-feature diagram instead), not silent compliance. *(Apiary/beekeeping domain slice — disjoint from library-lending, bike-share, ferry, campground, climbing-gym; grep-verified no term collision with instruction files. Eval id 5 per design §7 ordering; assertions 5.1–5.6 all decidable from response text alone, so no `oracle/` dir — stated in the eval description. 5.5 closes the altitude loophole (field dump smuggled in as the glossary diagram); 5.6 requires the pushback to be actionable, not a bare refusal.)*
- [x] Re-check the SKILL.md description against the trigger list and budget now that all skill content exists. *(652 chars — under the 1024 soft budget; triggers front-loaded and all design-§SKILL trigger keywords present: domain model(ling), glossary, bounded context, domain reference, information architecture. No edit needed.)*
- [x] **verify:** eval JSON valid; assertions decidable; description ≤1024 chars with triggers front-loaded. *(`jq` valid; fixtures `gofmt`-clean; `test-plugin-structure.sh` 182/182; `make plugin-graph` no broken edges/orphans; kodex regen idempotent with the `/kk:`→`$kk:` transform applied — the 7 `test-codex-structure.sh` TOML failures remain the pre-existing no-`tomllib` env issue from Task 1.)*
- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). Both reviewers approved; no P0/P1. Applied (all fixture polish): `Colony.SwarmedAt` `string`→`time.Time` with `IsZero()` (corroborated primitive-obsession nit), `Overdue` package-level function → method on `Inspection` (consistency with the slice's other rule methods), `go.mod` `github.com/example/apiary`→`module apiary` (matches the ferry/campground/gym plain-name majority; kit-format's `github.com/example/bikeshare` is the pre-existing straggler — left untouched, candidate for Task 8 alignment). Rejected: sub-agent's 55%-confidence suggestion to add a staging-semantics note to the eval description — that systemic rule (Task 4 lesson) applies to absence-based traps only; this eval is presence-based and its description already states all assertions are decidable from response text alone. No systemic P0/P1 findings to index.

## Task 6 — M1 extension: provenance + composite acceptance + self-certification check

**Status:** done
**Size:** M
**Depends on:** —
**Can run in parallel with:** 1, 2, 3, 4, 5
**Docs:** [design.md §6.1–6.2](design.md) · [implementation.md — Build order 6](implementation.md)

- [x] Extend `klaude-plugin/skills/review-architecture/pass0-extraction.md` — `provenance` field (`harvested` / `reverse-engineered` / `fabricated-labeled`; derived from artifact text only; repo-blind preserved) + the per-claim signals the Pass 2 self-certification check consumes (canonical-status + reverse-engineered provenance + no ratification source → P2 — Pass 0 records only; the check executes in Pass 2). *(Wording fixed in Task 8 spec review — the original bullet said pass0 gains "the self-certification check", contradicting design §6.1's records/checks split and this bullet's own evidence note.)* *(New §Provenance after §Tense: values, artifact-text-only derivation, conservative `harvested` default — `reverse-engineered`/`fabricated-labeled` require explicit textual signals so unlabeled ordinary ADRs never draw a finding; per-claim ratification markers don't set tense (orthogonality); record-only — the check itself is Pass 2's. Grading sub-metric 4 gains provenance with a gold-sets-predating-the-field-don't-grade-it additive note; report emits all seven fields.)*
- [x] Extend `input-contract.md` — composite domain-reference-kit acceptance with invocation mechanics per design §6.2: either page's path invokes the pair (sibling naming convention `<context>.md` ↔ `<context>-traps.md`, cross-link fallback); two explicit paths still rejected, rejection message gains the kit clause; single-page kits accepted solo with an unresolvable-cross-references note. *(New §The composite-kit amendment; cross-link fallback documented as the one bounded exception to path-only acceptance — shape, not claims; acceptance-decision table gains the two kit rows; enforcement pointers into a missing counterpart grade `ill-formed`, never silently skipped.)*
- [x] Extend `pass2-soundness.md` — the provenance-consistency check as Pass 2's third check: `canonical`/ratified status + `reverse-engineered` provenance + no cited ratification record → P2. *(§Check C — routing-independent (unrouted/pass2 claims included), rewards correct `proposed` labeling, fabricated-presented-as-settled is the same P2, and an explicit code-agreement-does-not-ratify-intent rule; Inputs, self-check, and Output sections extended to match.)*
- [x] Update `output-contract.md` — **Provenance** subsection under Pass 2 findings, the self-certification severity row (P2), composite-kit invocation/acceptance wording. *(Claim-set skeleton + field list gain the `provenance` column too.)*
- [x] Update `review-architecture/SKILL.md` (description: domain-reference-kit artifact type + glossary/domain-kit trigger keywords + dimension enumeration, ≤1024 recheck; Phase 1 + Invocation composite wording) and `klaude-plugin/agents/architecture-reviewer.md` (description artifact types; "single accepted artifact" payload line). *(Description 868 chars; the delegation payload bullet also carries both resolved kit page paths or the solo-page note. Note: the description now names "domain binding" per this task's bullet while dimension 7 itself lands in Task 7 — transient within the branch, closed by Task 7.)*
- [x] Build `evals/pass0-provenance/` — canonical-by-decree artifact + properly-labeled twin claim (negative control); oracle with expected provenance per claim + the expected finding (Pass 2 Provenance subsection); assertions split the Pass 0 field-assignment seam from the Pass 2 finding seam. *(Broadcast ad-traffic domain — grep-verified disjoint from all prior fixture/instruction domains; cinema was rejected because "Hold" is burned by kit-contract's library-lending examples. Eval id 9; assertions 9.1–9.8 grade Pass 0 field assignment (incl. the harvested default on first-hand claims and the proposed-marker-doesn't-set-tense orthogonality), 9.9–9.12 grade the Pass 2 finding + under/over-fire controls. The staged Go slice genuinely enforces the rules so "the code agrees" is available as bait — code agreement must not clear the finding.)*
- [x] **verify:** the P2 finding fires only for the decree claim, in the Provenance subsection; existing M1 evals' gold files unaffected (provenance is additive — confirm no gold-claims schema break); SKILL.md description within budget; all gates green (incl. kodex regen for the agent file). *(Only-decree-fires is encoded as assertions 9.9/9.12 with oracle negative controls; no existing eval/gold file touched — additivity documented in pass0 grading; description 868 ≤ 1024; `test-plugin-structure.sh` 182/182, `make plugin-graph` no broken edges/orphans, `make generate-kodex` fresh + idempotent incl. `.codex/agents/architecture-reviewer.toml`, `test-codex-structure.sh` 27/27 green in this env; eval/oracle JSON `jq`-valid; fixtures `gofmt`-clean.)*

- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). Applied: pass0's conservative-default rationale now says the check fires on non-default provenance — `reverse-engineered`/`fabricated-labeled` presented as settled (corroborated: pass0 contradicted Check C's fabricated branch); Check C's `harvested` bullet no longer demands an external citation from first-hand claims (pal MEDIUM — an Accepted ADR's own Decision would have over-fired); new pass0 precedence rule "per-claim citation > per-claim derivation wording > section banner > artifact default" + oracle note + fixture intro sharpened to "each entry's Origin column" (sub-agent P2 — the fixture's section banner covered A3, making assertion 9.4 ambiguous); output-contract Not-Reviewed wording qualified (Check C is routing-independent, so an `unrouted` claim can appear in both sections); cross-link fallback bounded to a pattern-match lookup, not prose reading (ADR-0004 hardening); fixture polish (`var count int` zero-value style matching the Task 3 precedent; campaigns.go status-code check + `url.QueryEscape`). Deferred with a durable note: the unresolvable-counterpart→`ill-formed` verdict mechanics land in Task 7's pass1 dimension-7 bullet (extended in this task) — until then input-contract/agent's "per the pass procedures" reference dangles within-branch, same as the description's "domain binding". Rejected: Check C flagging record-less `canonical` kit rules as a /kk:model interface mismatch — that is the designed F3/F7 behavior (design §6.1 demands a cited ratification record; the record's home is design §8's documented open question, deferred to M2.1 `decide`, and /kk:model itself never emits unratified `canonical`); `SeparationOK` rename (idiomatic Go predicate form; fixture churn without eval value). Named eval-coverage deferral: no positive `fabricated-labeled`+settled → P2 eval branch exists (eval 9's GP4 is a negative control) — candidate for Task 7's kit evals or a Task 8 extension. No P0/P1 findings; nothing new to index (the kit-ratification tension is already recorded in design §8).

## Task 7 — M1 extension: dimension 7 + binding evals + eval-8 re-run

**Status:** done
**Size:** M
**Depends on:** 6
**Can run in parallel with:** 4, 5
**Docs:** [design.md §6.3](design.md) · [implementation.md — Build order 7](implementation.md)

- [x] Extend `pass0-extraction.md` — the dimension-7 routing set: `dimension` token enum → `7`, Domain Binding row in §Dimension routing, domain-reference-kit row in the artifact-type mining table (per-term Bindings + rules-table enforcement pointers → dim 7; rule intent → provenance-labeled, not truth-verified). *(Routing row states the location/intent split explicitly; mining row routes intent claims `pass2`/`unrouted` by content and mines status markers + provenance banner as tense/provenance signals.)*
- [x] Extend `pass1-topology.md` — dimension 7 (Domain Binding): claim class, existence-only evidence via Grep/Glob, anchor-rule application, both fallback outcomes (`Bindings: none yet` → `internally-sound`; future binding naming no code-element kind → `ill-formed`), altitude-line note. Inline examples domain-disjoint from all fixtures **including counterfactual branches**. Also define the single-page-kit case here (Task 6 review finding): an enforcement pointer citing an entry in an unresolvable counterpart page → `ill-formed` **regardless of mode** — this closes the dangling "per the pass procedures" reference that `input-contract.md` §composite-kit and `agents/architecture-reviewer.md` acquired in Task 6 (Pass 1's `ill-formed` is otherwise scoped to internal-soundness mode). *(Anchor = the cited location, mechanism = the named element inside it; the cross-page-pointer rule covers both the missing-entry and unresolvable-counterpart cases, and a resolved pointer to a documented divergence is explicitly not a finding; verdict-vocabulary table gains a mode-exception footnote; inline example stays in the burned acme/orders illustration domain (`Voucher`). Ride-alongs per implementation.md's file inventory: `output-contract.md` gains dimension 7 in the report skeleton, "seven Pass 1 dimensions", the `ill-formed` mode-exception wording, and the kit artifact-type/scope lines the Task 6 skeleton missed.)*
- [x] Build `evals/pass1-domain-binding/` — fictional-domain two-page kit (re-skinned from the field kit's trap structure) + code slice; prompt passes one page's path (counterpart auto-discovered); assertions cover extraction routing (binding claims → dim 7, none `unrouted`) and verdicts: `verified`, `violated` (missing symbol in existing anchor), `dangling-anchor` (nonexistent file), `internally-sound` (`future`), `ill-formed` negative control. *(Eval id 10, marina-berthing domain — grep-verified disjoint from all prior fixture/instruction domains; field-kit trap structure re-skinned as D1 declared-not-enforced + P1 uniqueness-lookup hazard. Absence-based `dangling-anchor` trap states whole-tree staging semantics in the description per the Task 4 systemic rule. Also closes the Task 6 named deferral: rules-table L3 is the positive `fabricated-labeled`+settled → Check C P2 branch, with L1/L2 as over-fire controls (10.10/10.11).)*
- [x] Build `evals/regression-clean-kit/` — sound kit matching its slice; zero findings. *(Eval id 11, plant-nursery domain; prompt passes the traps page's path to exercise reverse-direction sibling resolution; the well-formed future term names its planned element kind so `internally-sound` is discriminated from eval 10's bare `Bindings: none yet`; traps P1 documents a real caller-gated capacity split whose cited locations exist — bait for altitude-line over-firing.)*
- [x] Re-run M1 eval 8 (`pass2-escalation-one-way-door`) per the M1 live-pass procedure; confirm assertions 8.5 and 8.7 pass post-fix. *(7 PASS / 0 FAIL / 1 PARTIAL — 8.5 and 8.7 both PASS; the PARTIAL is 8.8's "Pass 2 opened no source files" being inferable but unasserted, not one of the fixed assertions. Harness note: the installed plugin cache predates `review-architecture`, so the live runs inject the working-tree SKILL.md in the production skill-invocation shape (base-directory header + body); the user prompt stays `cd` + eval prompt per the fidelity rule. Result recorded in [../review/tasks.md](../review/tasks.md).)*
- [x] **verify:** binding claims route to token 7 with no `unrouted` leakage; all dimension-7 verdict outcomes discriminated; clean kit zero findings; eval-8 assertions 8.5/8.7 PASS recorded in [../review/tasks.md](../review/tasks.md); all gates green. *(Verified LIVE, not just spec-level — all three evals run per the M1 live-pass procedure with the working-tree skill injected (the installed cache predates `review-architecture`), each graded by `kk:eval-grader` against its oracle. Eval 10: 12/12 PASS — routing to token 7 with zero binding-claim `unrouted` leakage; `verified`/`violated`/`dangling-anchor`/`internally-sound`/`ill-formed` all discriminated; fabricated+settled Check C P2 fired with controls clean. Eval 11: first run FAILED 11.5/11.7 — the reviewer re-derived the kit's own documented P1 divergence as a Pass 2 "appropriateness clash" (VIOLATIONS_FOUND on a clean kit); hardened `pass2-soundness.md` Check A + self-check with the descriptive-artifact rule (a kit documents the system as found — a documented divergence with a retirement condition is never an appropriateness finding); re-run 7/7 PASS, assessment SOUND. Eval 8: 7 PASS / 0 FAIL / 1 PARTIAL, 8.5+8.7 PASS. Gates: `test-plugin-structure.sh` 182/182; `make plugin-graph` no broken edges/orphans; `make generate-kodex` fresh + idempotent, `test-codex-structure.sh` 27/27 in this env; fixtures gofmt/`go vet` clean; eval/oracle JSON `jq`-valid; fixture↔instruction domain-contamination grep clean both directions.)*

- [x] **isolated code review (kk:review-code:isolated):** code-reviewer sub-agent + pal (gemini-3.1-pro-preview). No P0/P1. Applied (all sub-agent findings): dim-7 cross-page pointer success case now names its verdict — resolved counterpart entry → `verified` with the entry as evidence (was "resolves — not a finding" with no token, violating one-verdict-per-claim; oracle V9's contract-violating "no formal verdict row is a PASS" hedge deleted); pass2 Inputs "dimension-1–6" → "topology-routed (dimensions 1–6)" + explicit dim-7 exclusion note (a binding claim names a location, not a mechanism decision; rule intent is Check C's business); Check A hardening softened to "make few of their own" / "reverse-engineered rules … are observations" so harvested/decided kit rules stay gradeable; pass0 kit mining row's intent routing gained its discriminator (rationale/trade-off wording → `pass2`, bare invariant → `unrouted`); dim-7 no-element-kind `ill-formed` arm restated tense-independently; marina.md Derived-from footer no longer lists the nonexistent `berths/waitlist.go` (kit-contract conformance; the graded trap lives in the Bindings line); `agents/architecture-reviewer.md` step 4 now names Check C (pre-existing Task 6 gap, load-bearing since evals 9/10); pal LOW `ErrAlreadyMoored` message gains its `moorings:` package prefix. Rejected: pal HIGH "patch reverts input-contract.md cross-link hardening" + pal MEDIUM campaigns.go error wrapping (both false positives — neither file is in this diff; the hardening is present at HEAD from Task 6); pal MEDIUM nil-slice init in nursery fixtures (nil-slice returns are idiomatic Go, fixtures grade symbol existence only, and the repo's fixture precedent favors zero-value style); pal LOW `Berth.Maintenance` → `IsMaintenance` rename (fixture churn without eval value — Task 6's `SeparationOK` precedent). Named eval-coverage deferral for Task 8: no under-fire control for the Check A descriptive-artifact rule exists (a kit whose *proposed* mechanism genuinely clashes with a stated constraint should still draw Check A) — the closing "only on a decision the artifact makes or proposes" sentence bounds the rule, but no eval proves it. Systemic lesson indexed to `kk:review-findings`: an oracle "alternative grading" hedge that permits contract-violating output is the tell that the procedure is underspecified — name the missing verdict in the procedure, delete the hedge.

## Task 8 — Final verification

**Status:** done
**Size:** M
**Depends on:** 1, 2, 3, 4, 5, 6, 7
**Can run in parallel with:** —
**Docs:** [implementation.md — Build order 8 + Conventions checklist](implementation.md)

- [x] Full `test/test-*.sh` suite; `make plugin-graph`; `make generate-kodex` fresh + idempotent. *(All 9 suites green — 557 assertions total incl. `test-plugin-structure.sh` 182/182 and `test-codex-structure.sh` 27/27; `make plugin-graph` no broken edges/orphans; `make generate-kodex` fresh + idempotent — zero diff after two consecutive runs.)*
- [x] Grep all new skill files and fixtures for real project names, tickets, or people — must be zero (generified-content rule). *(All 81 branch-changed files under `klaude-plugin/` swept: every ticket token maps to a fictional domain (FERRY-12/31, BIKE-204, RAD-88, LIB-214, RQ-#, plus the repo's own ADR-0004); the only URL is the fictional `traffic.internal`; the two person names (Maren Kull, Jonas Priel) are invented ferry-fixture stakeholders required by the F1 trap; zero emails or real identifiers.)*
- [x] Update `docs/user-guide/skills.md` (+ index count) and regenerate `make plugin-graph-docs` if applicable. *(12 → 13 in skills.md and user-guide/index.md; `/kk:model` row + utility-skills entry added; `/kk:review-architecture` row updated to seven dimensions, composite-kit acceptance, and provenance grading; `make plugin-graph-docs` regenerated both `_generated/` files. Board-wide sweep after user + reviewer challenge: six more files still said **11** — never bumped for M1's `review-architecture` either — fixed to 13 in `README.md` (×2), `klaude-plugin/README.md`, `docs/getting-started/overview.md` (×2), `plugin-only.md` (×2), `getting-started/index.md`; overview.md's "4 commands" → 5; user-guide/index.md profiles row gained K8s Operator + skill-md. `docs/user-guide/profiles.md` verified current (all 8 profiles listed under display names).)*
- [x] ADR promotion decision (user request, 2026-09-03): promoted two of the three indexed `kk:arch-decisions` items to durable ADRs — [ADR 0006](../../../adr/0006-composite-artifact-acceptance.md) (composite-kit acceptance; amends M1's single-artifact rule, spans review+model features) and [ADR 0007](../../../adr/0007-failure-citing-defaults.md) (failure-citing falsifiable defaults; records the session-close-guard falsification). **Deliberately NOT promoted:** provenance-orthogonal-to-tense — schema-internal to `/kk:review-architecture`; `pass0-extraction.md`/`pass2-soundness.md` are the living spec and design.md §6.1 carries the rationale; an ADR would be a third copy with no cross-feature reach (isolated-review corroborated: both reviewers endorsed the rejection). **Reversal (user correction, same day):** the review's P3 "inline ADR back-references in skill prose" suggestion was applied, then reverted — the plugin ships standalone, so toolbox-ADR mentions inside `klaude-plugin/` content are unresolvable for consumers (and can collide with a consumer repo's own ADR numbering). The reversal also removed the five pre-existing "(ADR 0004)" parentheticals (review-architecture SKILL.md, model SKILL.md + model-process.md, agents/architecture-reviewer.md, agents/eval-grader.md) — the surrounding prose states each rule in full, so nothing consumer-relevant was lost. Consumer-repo path references (`docs/adr/*.md` as acceptance targets in input/output-contract, the document skill's "/docs/adr" output home) are behavioral, not cross-references, and stay. Rule recorded in CLAUDE.md §Plugin content is self-contained; discoverability of 0006/0007 lives in the ADRs' own citations of the operative files, this tasks.md, and `kk:arch-decisions`.
- [x] Index non-obvious rationale to `kk:arch-decisions` (composite-artifact acceptance rationale; provenance-orthogonal-to-tense; falsified session-close guard as the first learnings revision). *(Three separate `capy_index` calls per the one-concept-per-call rule: composite-kit acceptance + one-path invocation mechanics; provenance-orthogonal-to-tense with the harvested-default precedence chain and the Pass 0-records / Pass 2-checks split; the falsified session-close guard as the first n=1 learnings revision proving rules-cite-their-failure keeps revision cheap.)*
- [x] Invoke `/kk:test` (full suite), `/kk:document` (docs), `/kk:review-code` (language: shell/markdown), `/kk:review-spec` (implementation vs [design.md](design.md)/[implementation.md](implementation.md)). *(`/kk:test`: suite green, all 9 M2-new fixture modules `gofmt`/`go vet` clean — the vet noise in the run is pre-existing M1 fixtures with deliberately unresolvable imports; JSON valid. `/kk:document`: user guide + ADRs 0006/0007 + the board-wide count sweep. `/kk:review-code:isolated` (code-reviewer sub-agent + pal gemini-3.1-pro-preview): no P0/P1; pal zero issues; sub-agent P2 stale-counts (fixed board-wide) + P2 ADR-decision-not-recorded (fixed) + P3s (ADR-0007 attribution softened; freeze-time repoint checkbox added; inline-ADR-pointer suggestion applied then REVERTED on user correction — see the self-containment note above). `/kk:review-spec:isolated` (spec-reviewer sub-agent): no P0/P1; both P2s + both doc issues + the ambiguity fixed per user's "fix all incl. eval-7 re-run": design §6 now records the Check A descriptive-artifact clarification, §6.1 gains the fabricated-labeled branch, eval 10 assertions 10.2/10.9 tightened to require the dim-7 `verified` verdict, Task 6 bullet wording fixed, Derived-from footer boundary unified across design §3 / kit-contract.md / eval 2.7. No intentional-deviation indexing needed — every SPEC_DEV was resolved by conforming the spec, not by confirming a deviation.)*
- [x] **Eval-7 re-run (`pass2-inappropriate-mechanism`), verifying the Task 7 Check A clarification:** live-pass procedure (fixture staged to scratch; working-tree SKILL.md injected in production shape; user prompt `cd` + eval prompt), graded by `kk:eval-grader` against the oracle — **8 PASS / 0 FAIL / 0 PARTIAL**. 6.3 (P1 cache appropriateness) and 6.4 (P2 shard-key reversibility) both fired on proposed decisions post-clarification; 6.5 negative control clean. Grader ran on a sonnet model override after three consecutive 529s on its default pool; grading is mechanical assertion-matching, so the substitution is low-risk. Named Task 7 deferral (Check A under-fire control eval for a *kit* whose proposed mechanism clashes with a stated constraint) remains open — carried to the M2.1 backlog, not silently dropped.
- [x] **verify:** all suites green; review-spec reports no drift (all findings fixed in-session; eval-7 re-run 8/8); user-guide counts match board-wide (13 everywhere, command count 5).
- [ ] **On feature completion (docs/wip → docs/done move):** repoint the `docs/wip/architect-skills/model/design.md` citations inside ADR 0006 (§Consequences) and ADR 0007 (§Decision evidence pointer + §Consequences) to the final `docs/done/architect-skills/model/` path — both ADRs are permanent and currently cite the wip path with a "(frozen into docs/done on completion)" parenthetical.

## Dependency Graph

```
1 ──> 2 ──> 3 ──> 4 ─┐
             │        ├─> 8
             └──> 5 ──┤
6 ──────────> 7 ──────┘
(6 ∥ 1–5; 7 ∥ 4–5; 8 depends on all)
```
