{
  "id": 3,
  "name": "k8s-helm-chart",
  "description": "A Helm chart diff (Chart.yaml + values.yaml + templated Deployment + _helpers.tpl) activates the k8s profile, helm-checklist, and reliability-checklist.",
  "skills": ["review-code"],
  "prompt": "I've staged the following changes. Run `/kk:review-code` against them.",
  "trap": "Model doesn't recognize templates as Kubernetes manifests because `{{ }}` placeholders make them technically invalid YAML, or misses the `values.yaml` / `Chart.yaml` adjacency rule, or spuriously loads `kustomize-checklist.md` alongside helm.",
  "files": [
    "test-files/Chart.yaml",
    "test-files/values.yaml",
    "test-files/templates/deployment.yaml",
    "test-files/templates/_helpers.tpl"
  ],
  "assertions": [
    { "id": "3.1", "text": "Identifies `k8s` as the active profile — via the `Chart.yaml` filename signal (Helm chart root)." },
    { "id": "3.2", "text": "Recognizes `values.yaml` as a Helm values file by filename-adjacency to `Chart.yaml` in the same directory." },
    { "id": "3.3", "text": "Recognizes `templates/deployment.yaml` and `templates/_helpers.tpl` as Helm templates (`.yaml`/`.tpl` under a `templates/` directory whose ancestor contains `Chart.yaml`)." },
    { "id": "3.4", "text": "Loads the four always-load checklists from `profiles/k8s/review-code/`." },
    { "id": "3.5", "text": "Loads `helm-checklist.md` because the diff contains `Chart.yaml`." },
    { "id": "3.6", "text": "Loads `reliability-checklist.md` because the template declares a literal `kind: Deployment`." },
    { "id": "3.7", "text": "Does NOT load `kustomize-checklist.md`." },
    { "id": "3.8", "text": "Emits findings grouped by `(k8s, <checklist>)`." }
  ]
}
