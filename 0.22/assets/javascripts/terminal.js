/* Intentionally empty — all landing page JS is inline in home.html.
   This file exists so mkdocs.yml's extra_javascript reference doesn't 404.
   Future non-homepage JS (e.g. heading effects) can go here. */
