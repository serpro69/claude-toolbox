{
  "mcpServers": {
    "capy": {
      "command": "bash",
      "args": [
        "-c",
        "for p in $(which capy 2>/dev/null) $HOME/.local/bin/capy /opt/homebrew/bin/capy /usr /local/bin/capy capy; do [ -x \"$p\" ] && exec \"$p\" serve && break; done; echo 'capy not found' >&2; exit 1"
      ]
    }
  }
}
