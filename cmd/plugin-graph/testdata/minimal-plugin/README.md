# Minimal plugin fixture

A stripped-down plugin tree used by the plugin-graph integration test. A
`README.md` at the plugin root is an entry point and must NOT be flagged as an
orphan even though nothing links to it — this file pins that exclusion.
