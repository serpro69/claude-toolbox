# Profile detection

Minimal fixture stand-in for the real detection procedure. The walker only needs
the §Known profiles list below so `NewParseContext` can derive `KnownProfiles`
(it fails the build if the list is empty).

## Known profiles

- `sample`
