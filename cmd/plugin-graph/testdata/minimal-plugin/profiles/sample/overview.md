# Sample profile overview

Human-readable summary of the sample fixture profile. It sits inside the profile
directory, so the walker normalizes it into the `profiles/sample/` artifact node
rather than giving it a node of its own.
